apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: observer-app-ingress
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: observer-app-platform-service
            port:
              number: 80